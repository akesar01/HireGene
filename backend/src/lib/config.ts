// ─── App-wide configuration ──────────────────────────────────────────────────

/** Job posts auto-expire after this many days. Scrape window matches this value. */
export const JOB_EXPIRY_DAYS = 30;
